# Local development profile — packaged in the JAR but only active with the 'dev' profile
# This profile is never activated in production (no SPRING_PROFILES_ACTIVE=dev in docker-compose/Cloud Run)
management.endpoint.health.show-details=always
app.cors.allowed-origins=http://localhost:*
