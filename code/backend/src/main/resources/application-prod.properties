# Production profile — active in Cloud Run prod (SPRING_PROFILES_ACTIVE=prod)
# Conexión a BD viene de variables de entorno; BookSeederProd activo en prod para datos iniciales.
management.endpoint.health.show-details=never
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:https://atreyu-library.pakodiaz.dev}

# Hikari — resiliencia de conexiones para Supabase/PgBouncer
# socketTimeout: falla rápido si la conexión muere (no colgar indefinidamente)
spring.datasource.hikari.data-source-properties.socketTimeout=60
spring.datasource.hikari.keepalive-time=30000
# Deshabilita idle_in_transaction_session_timeout en cada conexión del pool;
# evita que Supabase cierre la conexión mientras Hibernate hace INSERTs secuenciales
spring.datasource.hikari.connection-init-sql=SET idle_in_transaction_session_timeout = 0

# Sentry
sentry.dsn=${SENTRY_DSN_BACKEND}
sentry.environment=prod
sentry.traces-sample-rate=0.0
